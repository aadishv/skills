pnpm dlx tsx "$(dirname "${BASH_SOURCE[0]}")/../../walkthrough/scripts/cli.ts" --upload "$@"
# aadish: i spent so much time into that cli script that it works well for arbitrary md files. who would've thought lol
